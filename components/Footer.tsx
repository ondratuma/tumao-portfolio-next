import styles from "../styles/Footer.module.scss";
import React, {EventHandler} from "react";

export const Footer = () => {

    return (
        <footer className={styles.footer}>
                    Does website run in the cloud?
        </footer>
    )
}